<div class="<%= grid_classes %>">
  <% @posts.each do |post| %>
    <%= render Card::PostComponent.new(post: post) %>
  <% end %>
  <% if @posts.empty? %>
    <div class="text-center py-12">
      <p class="text-[#565f89] text-lg">No posts found.</p>
    </div>
  <% end %>
</div>
