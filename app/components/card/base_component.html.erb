<div class="<%= card_classes %>" data-controller="<%= stimulus_controllers %>">
  <div class="card-content">
    <%= content %>
    <% if badges? %>
      <div class="flex flex-wrap gap-2 mt-4">
        <%= badges %>
      </div>
    <% end %>
  </div>
</div>
