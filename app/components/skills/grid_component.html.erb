<div class="skills-section">
  <% if @title %>
    <h2 class="text-2xl sm:text-3xl font-bold text-[#7aa2f7] font-heading text-center mb-8">
      <%= @title %>
    </h2>
  <% end %>
  <div class="<%= grid_classes %>">
    <%= categories %>
  </div>
</div>
