<%# Projects Index with Tokyo Night theme %>
<div class="min-h-screen bg-[#1a1b26] text-[#a9b1d6] py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto w-full">
    <h1 class="text-[#7aa2f7] text-4xl font-bold mb-12">Projects</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 isolate">
      <%= render partial: "project", collection: @projects, as: :project %>
    </div>
  </div>
</div> 