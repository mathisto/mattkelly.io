<div class="min-h-screen bg-[#1a1b26] text-[#c0caf5] p-8">
  <div class="max-w-7xl mx-auto">
    <!-- Header -->
    <div class="mb-8 flex items-center justify-between">
      <div>
        <h1 class="text-4xl font-bold bg-gradient-to-r from-[#7aa2f7] to-[#bb9af7] bg-clip-text text-transparent">
          Parse History
        </h1>
        <p class="text-[#a9b1d6] mt-2">View all utterance parsing attempts</p>
      </div>
      <%= link_to "← Dashboard", scribe_dashboard_path, class: "text-[#7aa2f7] hover:text-[#7dcfff]" %>
    </div>
    <!-- Ingestions Table -->
    <div class="bg-[#24283b] border border-[#3b4261] rounded-lg overflow-hidden">
      <% if @ingestions.any? %>
        <table class="w-full">
          <thead class="bg-[#1a1b26]">
            <tr>
              <th class="px-4 py-3 text-left text-xs font-medium text-[#a9b1d6] uppercase tracking-wider">Utterance</th>
              <th class="px-4 py-3 text-left text-xs font-medium text-[#a9b1d6] uppercase tracking-wider">Status</th>
              <th class="px-4 py-3 text-left text-xs font-medium text-[#a9b1d6] uppercase tracking-wider">Model</th>
              <th class="px-4 py-3 text-left text-xs font-medium text-[#a9b1d6] uppercase tracking-wider">Metrics</th>
              <th class="px-4 py-3 text-left text-xs font-medium text-[#a9b1d6] uppercase tracking-wider">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @ingestions.each do |ingestion| %>
              <%= render Scribe::IngestionRowComponent.new(ingestion: ingestion) %>
            <% end %>
          </tbody>
        </table>
        <!-- Pagination -->
        <div class="px-4 py-3 bg-[#1a1b26] border-t border-[#3b4261]">
          <%= paginate @ingestions %>
        </div>
      <% else %>
        <div class="p-12 text-center">
          <p class="text-[#565f89] text-lg mb-4">No ingestions yet</p>
          <%= link_to "Go to Dashboard", scribe_dashboard_path, class: "text-[#7aa2f7] hover:text-[#7dcfff]" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
