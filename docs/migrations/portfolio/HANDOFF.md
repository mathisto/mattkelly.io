# Portfolio Migration Handoff

## Current Phase

Static portfolio foundation implemented on the migration branch. Deployment and infrastructure activation remain pending and attended.

## Confirmed

- `mattkelly.io` is the future canonical portfolio repository.
- Rails becomes historical after a stabilization window.
- Production currently runs through Cloudflare Tunnel and Caddy in the home lab.
- Ordinary portfolio pages are generated by Quartz and served by Caddy.
- Quartz and Soul expose anonymous shared-state controls that require containment.
- `/api/recent.json` exposes exact recent visitor paths and must be replaced.
- The edge fallback will be a tiny explicit degraded-state page, not a full mirror.
- Solar ingestion will run once daily after provider access is supplied.
- The voice-assistant project is removed from portfolio scope.
- Existing reference publication consent is confirmed.
- Home-lab backups are deferred; alerting and ingress restrictions remain in scope.
- The hosted Quartz source no longer routes `/load` or `/load/*`, and Caddy proxies only exact `/api/info`.
- The Soul source no longer routes anonymous mutation controls, detailed stats, full-pool task scans, or exact recent paths.
- Soul's retained anonymous JSON is limited to version, ticks, alive/ready/spawned/exited task counts, and heartbeat, polled every five seconds.
- The portfolio source is repository-owned under `site/` and builds without Rails, Ruby, a database, Node, Astro, or browser framework dependencies.
- `/quartz` is an optional prebuilt artifact contract; the core portfolio remains buildable without it.
- The core layout uses system fonts and local assets with no third-party browser scripts or stylesheets.

## Existing Worktree Changes

- `mattkelly.io`: `app/controllers/scribe/utterances_controller.rb` was already modified and must remain untouched.
- `quartz`: the canonical checkout contains extensive unrelated modified and untracked stabilization work. Security implementation must use an isolated worktree or wait for the owner to reconcile the active branch.

## Next Actions

1. Review the Phase 0 Quartz worktree at `/var/folders/_f/0x_m059s5gjdxhfclhhb1xfc0000gn/T/opencode/quartz-portfolio-phase0`.
2. Rebuild the real generated bare-metal assets and complete an ELF validation against the intended production lineage.
3. Verify the installed Soul Caddy and Cloudflare exposure without invoking mutations.
4. Apply the source patch and edge rules through an attended deployment.
5. Validate the Cloudflare fallback on a preview hostname.
6. Review and expand the foundation content/evidence inventory before the editorial redesign.

## Human Inputs Pending

- Solar provider name, API documentation, endpoint type, and credentials
- Whether Cloudflare Custom Errors are available on the current plan
- Which Quartz branch or commit should receive the reviewed endpoint hardening before deployment

## Phase 0 Worktree Evidence

Branch: `portfolio/phase-0-quartz-safety`

Modified files:

- `site/deploy/Caddyfile.snippet`
- `site/run.sh`
- `site/server.qz`
- `tools/baremetal/hello_x86.qz`

Verified:

- Hosted Quartz source compiles to LLVM IR and object code.
- Caddy formatting and diff checks pass.
- Active Soul inline JavaScript parses successfully.
- Static reachability review finds no public mutation, recent-path, detailed-stats, or full-pool task route.
- Independent adversarial review reports no medium-or-higher finding.

Blocked verification:

- Full bare-metal ELF generation requires the ignored generated `tools/baremetal/site_assets.qz` artifact and the intended production asset build.
- No live endpoint, QEMU, deployment, DNS, or Cloudflare mutation has been performed.
